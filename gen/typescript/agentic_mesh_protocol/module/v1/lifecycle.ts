// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.8.3
//   protoc               unknown
// source: agentic_mesh_protocol/module/v1/lifecycle.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Struct } from "../../../google/protobuf/struct.js";
import { SetupVersion } from "../../setup/v1/setup.js";

export const protobufPackage = "agentic_mesh_protocol.module.v1";

/**
 * ConfigSetupModuleRequest
 * This request is used to configure an existing setup version.
 *
 * Fields:
 * - setup_version: The setup version to configure.
 * - content: The new content to compute and/or configure.
 * - mission_id: Database ID of the Module to start
 */
export interface ConfigSetupModuleRequest {
  /** setup_version: The setup version to configure. */
  setupVersion?:
    | SetupVersion
    | undefined;
  /** content: The new content to compute and/or configure. */
  content?:
    | { [key: string]: any }
    | undefined;
  /** mission_id: Database ID of the Module to start */
  missionId: string;
}

/**
 * ConfigSetupModuleResponse
 * Returns the configured setup version.
 *
 * Fields:
 * - success: description flag of the operation.
 * - setup_version: The updated setup version entity.
 */
export interface ConfigSetupModuleResponse {
  /** success: description flag of the operation. */
  success: boolean;
  /** setup_version: The updated setup version entity. */
  setupVersion?: SetupVersion | undefined;
}

/**
 * StartModuleRequest
 * TODO: do we have to add secret_id ?
 *
 * Fields:
 *
 * - input: Structured input data for the module
 * - setup_id: Database ID of the setupused to start the module
 * - mission_id: Database ID of the Module to start
 */
export interface StartModuleRequest {
  /** input: Structured input data for the module */
  input?:
    | { [key: string]: any }
    | undefined;
  /** setup_id: Database ID of the setup used to start the module */
  setupId: string;
  /** mission_id: Database ID of the Module to start */
  missionId: string;
}

/**
 * StopModuleRequest
 *
 * Fields:
 *
 * - module_id: Database ID of the Module to stop
 */
export interface StopModuleRequest {
  /** job_id: Database ID of the job to stop */
  jobId: string;
}

/**
 * StartModuleResponse
 *
 * Returns:
 *
 * - success: Flag to indicate if the started/stopped request was successful
 * - output: Structured output data a job
 * - job_id: Database ID of the job  inside the Module that was queried
 */
export interface StartModuleResponse {
  /** success: Flag to indicate if the started request was successful */
  success: boolean;
  /** output: Structured output data of a started job (streamed) */
  output?:
    | { [key: string]: any }
    | undefined;
  /** job_id: Database ID of the job  inside the Module that was queried */
  jobId: string;
}

/**
 * StopModuleResponse
 *
 * Returns:
 *
 * - success: Flag to indicate if the started/stopped request was successful
 * - module_id: Database ID of the Module that was started/stopped
 */
export interface StopModuleResponse {
  /** success: Flag to indicate if the started/stopped request was successful */
  success: boolean;
  /** job_id: Database ID of the Module that was started/stopped */
  jobId?: string | undefined;
}

function createBaseConfigSetupModuleRequest(): ConfigSetupModuleRequest {
  return { setupVersion: undefined, content: undefined, missionId: "" };
}

export const ConfigSetupModuleRequest: MessageFns<ConfigSetupModuleRequest> = {
  encode(message: ConfigSetupModuleRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.setupVersion !== undefined) {
      SetupVersion.encode(message.setupVersion, writer.uint32(10).fork()).join();
    }
    if (message.content !== undefined) {
      Struct.encode(Struct.wrap(message.content), writer.uint32(18).fork()).join();
    }
    if (message.missionId !== "") {
      writer.uint32(26).string(message.missionId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ConfigSetupModuleRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseConfigSetupModuleRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.setupVersion = SetupVersion.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.content = Struct.unwrap(Struct.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.missionId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ConfigSetupModuleRequest {
    return {
      setupVersion: isSet(object.setup_version) ? SetupVersion.fromJSON(object.setup_version) : undefined,
      content: isObject(object.content) ? object.content : undefined,
      missionId: isSet(object.mission_id) ? globalThis.String(object.mission_id) : "",
    };
  },

  toJSON(message: ConfigSetupModuleRequest): unknown {
    const obj: any = {};
    if (message.setupVersion !== undefined) {
      obj.setup_version = SetupVersion.toJSON(message.setupVersion);
    }
    if (message.content !== undefined) {
      obj.content = message.content;
    }
    if (message.missionId !== "") {
      obj.mission_id = message.missionId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ConfigSetupModuleRequest>, I>>(base?: I): ConfigSetupModuleRequest {
    return ConfigSetupModuleRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ConfigSetupModuleRequest>, I>>(object: I): ConfigSetupModuleRequest {
    const message = createBaseConfigSetupModuleRequest();
    message.setupVersion = (object.setupVersion !== undefined && object.setupVersion !== null)
      ? SetupVersion.fromPartial(object.setupVersion)
      : undefined;
    message.content = object.content ?? undefined;
    message.missionId = object.missionId ?? "";
    return message;
  },
};

function createBaseConfigSetupModuleResponse(): ConfigSetupModuleResponse {
  return { success: false, setupVersion: undefined };
}

export const ConfigSetupModuleResponse: MessageFns<ConfigSetupModuleResponse> = {
  encode(message: ConfigSetupModuleResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.success !== false) {
      writer.uint32(8).bool(message.success);
    }
    if (message.setupVersion !== undefined) {
      SetupVersion.encode(message.setupVersion, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ConfigSetupModuleResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseConfigSetupModuleResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.setupVersion = SetupVersion.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ConfigSetupModuleResponse {
    return {
      success: isSet(object.success) ? globalThis.Boolean(object.success) : false,
      setupVersion: isSet(object.setup_version) ? SetupVersion.fromJSON(object.setup_version) : undefined,
    };
  },

  toJSON(message: ConfigSetupModuleResponse): unknown {
    const obj: any = {};
    if (message.success !== false) {
      obj.success = message.success;
    }
    if (message.setupVersion !== undefined) {
      obj.setup_version = SetupVersion.toJSON(message.setupVersion);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ConfigSetupModuleResponse>, I>>(base?: I): ConfigSetupModuleResponse {
    return ConfigSetupModuleResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ConfigSetupModuleResponse>, I>>(object: I): ConfigSetupModuleResponse {
    const message = createBaseConfigSetupModuleResponse();
    message.success = object.success ?? false;
    message.setupVersion = (object.setupVersion !== undefined && object.setupVersion !== null)
      ? SetupVersion.fromPartial(object.setupVersion)
      : undefined;
    return message;
  },
};

function createBaseStartModuleRequest(): StartModuleRequest {
  return { input: undefined, setupId: "", missionId: "" };
}

export const StartModuleRequest: MessageFns<StartModuleRequest> = {
  encode(message: StartModuleRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.input !== undefined) {
      Struct.encode(Struct.wrap(message.input), writer.uint32(10).fork()).join();
    }
    if (message.setupId !== "") {
      writer.uint32(18).string(message.setupId);
    }
    if (message.missionId !== "") {
      writer.uint32(26).string(message.missionId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StartModuleRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStartModuleRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.input = Struct.unwrap(Struct.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.setupId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.missionId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StartModuleRequest {
    return {
      input: isObject(object.input) ? object.input : undefined,
      setupId: isSet(object.setup_id) ? globalThis.String(object.setup_id) : "",
      missionId: isSet(object.mission_id) ? globalThis.String(object.mission_id) : "",
    };
  },

  toJSON(message: StartModuleRequest): unknown {
    const obj: any = {};
    if (message.input !== undefined) {
      obj.input = message.input;
    }
    if (message.setupId !== "") {
      obj.setup_id = message.setupId;
    }
    if (message.missionId !== "") {
      obj.mission_id = message.missionId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StartModuleRequest>, I>>(base?: I): StartModuleRequest {
    return StartModuleRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StartModuleRequest>, I>>(object: I): StartModuleRequest {
    const message = createBaseStartModuleRequest();
    message.input = object.input ?? undefined;
    message.setupId = object.setupId ?? "";
    message.missionId = object.missionId ?? "";
    return message;
  },
};

function createBaseStopModuleRequest(): StopModuleRequest {
  return { jobId: "" };
}

export const StopModuleRequest: MessageFns<StopModuleRequest> = {
  encode(message: StopModuleRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.jobId !== "") {
      writer.uint32(10).string(message.jobId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StopModuleRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStopModuleRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.jobId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StopModuleRequest {
    return { jobId: isSet(object.job_id) ? globalThis.String(object.job_id) : "" };
  },

  toJSON(message: StopModuleRequest): unknown {
    const obj: any = {};
    if (message.jobId !== "") {
      obj.job_id = message.jobId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StopModuleRequest>, I>>(base?: I): StopModuleRequest {
    return StopModuleRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StopModuleRequest>, I>>(object: I): StopModuleRequest {
    const message = createBaseStopModuleRequest();
    message.jobId = object.jobId ?? "";
    return message;
  },
};

function createBaseStartModuleResponse(): StartModuleResponse {
  return { success: false, output: undefined, jobId: "" };
}

export const StartModuleResponse: MessageFns<StartModuleResponse> = {
  encode(message: StartModuleResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.success !== false) {
      writer.uint32(8).bool(message.success);
    }
    if (message.output !== undefined) {
      Struct.encode(Struct.wrap(message.output), writer.uint32(18).fork()).join();
    }
    if (message.jobId !== "") {
      writer.uint32(26).string(message.jobId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StartModuleResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStartModuleResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.output = Struct.unwrap(Struct.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.jobId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StartModuleResponse {
    return {
      success: isSet(object.success) ? globalThis.Boolean(object.success) : false,
      output: isObject(object.output) ? object.output : undefined,
      jobId: isSet(object.job_id) ? globalThis.String(object.job_id) : "",
    };
  },

  toJSON(message: StartModuleResponse): unknown {
    const obj: any = {};
    if (message.success !== false) {
      obj.success = message.success;
    }
    if (message.output !== undefined) {
      obj.output = message.output;
    }
    if (message.jobId !== "") {
      obj.job_id = message.jobId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StartModuleResponse>, I>>(base?: I): StartModuleResponse {
    return StartModuleResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StartModuleResponse>, I>>(object: I): StartModuleResponse {
    const message = createBaseStartModuleResponse();
    message.success = object.success ?? false;
    message.output = object.output ?? undefined;
    message.jobId = object.jobId ?? "";
    return message;
  },
};

function createBaseStopModuleResponse(): StopModuleResponse {
  return { success: false, jobId: undefined };
}

export const StopModuleResponse: MessageFns<StopModuleResponse> = {
  encode(message: StopModuleResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.success !== false) {
      writer.uint32(8).bool(message.success);
    }
    if (message.jobId !== undefined) {
      writer.uint32(18).string(message.jobId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StopModuleResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStopModuleResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.jobId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StopModuleResponse {
    return {
      success: isSet(object.success) ? globalThis.Boolean(object.success) : false,
      jobId: isSet(object.job_id) ? globalThis.String(object.job_id) : undefined,
    };
  },

  toJSON(message: StopModuleResponse): unknown {
    const obj: any = {};
    if (message.success !== false) {
      obj.success = message.success;
    }
    if (message.jobId !== undefined) {
      obj.job_id = message.jobId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StopModuleResponse>, I>>(base?: I): StopModuleResponse {
    return StopModuleResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StopModuleResponse>, I>>(object: I): StopModuleResponse {
    const message = createBaseStopModuleResponse();
    message.success = object.success ?? false;
    message.jobId = object.jobId ?? undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
