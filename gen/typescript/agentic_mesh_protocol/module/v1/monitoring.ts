// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.8.3
//   protoc               unknown
// source: agentic_mesh_protocol/module/v1/monitoring.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "agentic_mesh_protocol.module.v1";

/**
 * ModuleStatus
 * TODO: adapt to the real status of the module
 */
export enum ModuleStatus {
  /** MODULE_STATUS_UNSPECIFIED - MODULE_STATUS_UNSPECIFIED is the default unspecified value */
  MODULE_STATUS_UNSPECIFIED = 0,
  /** MODULE_STATUS_STARTING - MODULE_STATUS_STARTING is the status when the module is starting */
  MODULE_STATUS_STARTING = 1,
  /** MODULE_STATUS_PROCESSING - MODULE_STATUS_PROCESSING is the status when the module is processing */
  MODULE_STATUS_PROCESSING = 2,
  /** MODULE_STATUS_CANCELED - MODULE_STATUS_CANCELED is the status when the module is canceled */
  MODULE_STATUS_CANCELED = 3,
  /** MODULE_STATUS_FAILED - MODULE_STATUS_FAILED is the status when the module is failed */
  MODULE_STATUS_FAILED = 4,
  /** MODULE_STATUS_EXPIRED - MODULE_STATUS_EXPIRED is the status when the module is expired */
  MODULE_STATUS_EXPIRED = 5,
  /** MODULE_STATUS_SUCCESS - MODULE_STATUS_SUCCESS is the status when the module is successful */
  MODULE_STATUS_SUCCESS = 6,
  /** MODULE_STATUS_STOPPED - MODULE_STATUS_STOPPED is the status when the module is stopped */
  MODULE_STATUS_STOPPED = 7,
  UNRECOGNIZED = -1,
}

export function moduleStatusFromJSON(object: any): ModuleStatus {
  switch (object) {
    case 0:
    case "MODULE_STATUS_UNSPECIFIED":
      return ModuleStatus.MODULE_STATUS_UNSPECIFIED;
    case 1:
    case "MODULE_STATUS_STARTING":
      return ModuleStatus.MODULE_STATUS_STARTING;
    case 2:
    case "MODULE_STATUS_PROCESSING":
      return ModuleStatus.MODULE_STATUS_PROCESSING;
    case 3:
    case "MODULE_STATUS_CANCELED":
      return ModuleStatus.MODULE_STATUS_CANCELED;
    case 4:
    case "MODULE_STATUS_FAILED":
      return ModuleStatus.MODULE_STATUS_FAILED;
    case 5:
    case "MODULE_STATUS_EXPIRED":
      return ModuleStatus.MODULE_STATUS_EXPIRED;
    case 6:
    case "MODULE_STATUS_SUCCESS":
      return ModuleStatus.MODULE_STATUS_SUCCESS;
    case 7:
    case "MODULE_STATUS_STOPPED":
      return ModuleStatus.MODULE_STATUS_STOPPED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ModuleStatus.UNRECOGNIZED;
  }
}

export function moduleStatusToJSON(object: ModuleStatus): string {
  switch (object) {
    case ModuleStatus.MODULE_STATUS_UNSPECIFIED:
      return "MODULE_STATUS_UNSPECIFIED";
    case ModuleStatus.MODULE_STATUS_STARTING:
      return "MODULE_STATUS_STARTING";
    case ModuleStatus.MODULE_STATUS_PROCESSING:
      return "MODULE_STATUS_PROCESSING";
    case ModuleStatus.MODULE_STATUS_CANCELED:
      return "MODULE_STATUS_CANCELED";
    case ModuleStatus.MODULE_STATUS_FAILED:
      return "MODULE_STATUS_FAILED";
    case ModuleStatus.MODULE_STATUS_EXPIRED:
      return "MODULE_STATUS_EXPIRED";
    case ModuleStatus.MODULE_STATUS_SUCCESS:
      return "MODULE_STATUS_SUCCESS";
    case ModuleStatus.MODULE_STATUS_STOPPED:
      return "MODULE_STATUS_STOPPED";
    case ModuleStatus.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/**
 * JobInfo
 *
 * Represents information about a single job
 */
export interface JobInfo {
  /** job_id: Database ID of the job inside the Module */
  jobId: string;
  /** job_status: Status of the individual job */
  jobStatus: ModuleStatus;
}

/**
 * GetModuleStatusRequest
 *
 * Fields:
 *
 * - job_id: Database ID of the Job Module to get status
 */
export interface GetModuleStatusRequest {
  /** job_id: Database ID of the Job Module to get status */
  jobId: string;
}

/**
 * GetModuleStatusResponse
 *
 * Returns:
 *
 * - success: Flag to indicate if the status request was successful
 * - status: Status of the Module
 * - job_id: Database ID of the job inside the Module that was queried
 */
export interface GetModuleStatusResponse {
  /** success: Flag to indicate if the status request was successful */
  success: boolean;
  /** status: Status of the Module */
  status: ModuleStatus;
  /** job_id: Database ID of the job inside the Module that was queried */
  jobId: string;
}

/**
 * GetModuleJobsRequest
 * TODO: adapt depending on the real way we will get the jobs
 *
 * Fields:
 *
 * - job_id: Database ID of the Job Module to get status
 */
export interface GetModuleJobsRequest {
}

/**
 * GetModuleJobsResponse
 *
 * Returns:
 *
 * - success: Flag to indicate if the status request was successful
 * - jobs: List of jobs with their respective IDs and statuses
 */
export interface GetModuleJobsResponse {
  /** success: Flag to indicate if the status request was successful */
  success: boolean;
  /** jobs: List of jobs with their respective IDs and statuses */
  jobs: JobInfo[];
}

function createBaseJobInfo(): JobInfo {
  return { jobId: "", jobStatus: 0 };
}

export const JobInfo: MessageFns<JobInfo> = {
  encode(message: JobInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.jobId !== "") {
      writer.uint32(10).string(message.jobId);
    }
    if (message.jobStatus !== 0) {
      writer.uint32(16).int32(message.jobStatus);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): JobInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseJobInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.jobId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.jobStatus = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): JobInfo {
    return {
      jobId: isSet(object.job_id) ? globalThis.String(object.job_id) : "",
      jobStatus: isSet(object.job_status) ? moduleStatusFromJSON(object.job_status) : 0,
    };
  },

  toJSON(message: JobInfo): unknown {
    const obj: any = {};
    if (message.jobId !== "") {
      obj.job_id = message.jobId;
    }
    if (message.jobStatus !== 0) {
      obj.job_status = moduleStatusToJSON(message.jobStatus);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<JobInfo>, I>>(base?: I): JobInfo {
    return JobInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<JobInfo>, I>>(object: I): JobInfo {
    const message = createBaseJobInfo();
    message.jobId = object.jobId ?? "";
    message.jobStatus = object.jobStatus ?? 0;
    return message;
  },
};

function createBaseGetModuleStatusRequest(): GetModuleStatusRequest {
  return { jobId: "" };
}

export const GetModuleStatusRequest: MessageFns<GetModuleStatusRequest> = {
  encode(message: GetModuleStatusRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.jobId !== "") {
      writer.uint32(10).string(message.jobId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetModuleStatusRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetModuleStatusRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.jobId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetModuleStatusRequest {
    return { jobId: isSet(object.job_id) ? globalThis.String(object.job_id) : "" };
  },

  toJSON(message: GetModuleStatusRequest): unknown {
    const obj: any = {};
    if (message.jobId !== "") {
      obj.job_id = message.jobId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetModuleStatusRequest>, I>>(base?: I): GetModuleStatusRequest {
    return GetModuleStatusRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetModuleStatusRequest>, I>>(object: I): GetModuleStatusRequest {
    const message = createBaseGetModuleStatusRequest();
    message.jobId = object.jobId ?? "";
    return message;
  },
};

function createBaseGetModuleStatusResponse(): GetModuleStatusResponse {
  return { success: false, status: 0, jobId: "" };
}

export const GetModuleStatusResponse: MessageFns<GetModuleStatusResponse> = {
  encode(message: GetModuleStatusResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.success !== false) {
      writer.uint32(8).bool(message.success);
    }
    if (message.status !== 0) {
      writer.uint32(16).int32(message.status);
    }
    if (message.jobId !== "") {
      writer.uint32(26).string(message.jobId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetModuleStatusResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetModuleStatusResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.jobId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetModuleStatusResponse {
    return {
      success: isSet(object.success) ? globalThis.Boolean(object.success) : false,
      status: isSet(object.status) ? moduleStatusFromJSON(object.status) : 0,
      jobId: isSet(object.job_id) ? globalThis.String(object.job_id) : "",
    };
  },

  toJSON(message: GetModuleStatusResponse): unknown {
    const obj: any = {};
    if (message.success !== false) {
      obj.success = message.success;
    }
    if (message.status !== 0) {
      obj.status = moduleStatusToJSON(message.status);
    }
    if (message.jobId !== "") {
      obj.job_id = message.jobId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetModuleStatusResponse>, I>>(base?: I): GetModuleStatusResponse {
    return GetModuleStatusResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetModuleStatusResponse>, I>>(object: I): GetModuleStatusResponse {
    const message = createBaseGetModuleStatusResponse();
    message.success = object.success ?? false;
    message.status = object.status ?? 0;
    message.jobId = object.jobId ?? "";
    return message;
  },
};

function createBaseGetModuleJobsRequest(): GetModuleJobsRequest {
  return {};
}

export const GetModuleJobsRequest: MessageFns<GetModuleJobsRequest> = {
  encode(_: GetModuleJobsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetModuleJobsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetModuleJobsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): GetModuleJobsRequest {
    return {};
  },

  toJSON(_: GetModuleJobsRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<GetModuleJobsRequest>, I>>(base?: I): GetModuleJobsRequest {
    return GetModuleJobsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetModuleJobsRequest>, I>>(_: I): GetModuleJobsRequest {
    const message = createBaseGetModuleJobsRequest();
    return message;
  },
};

function createBaseGetModuleJobsResponse(): GetModuleJobsResponse {
  return { success: false, jobs: [] };
}

export const GetModuleJobsResponse: MessageFns<GetModuleJobsResponse> = {
  encode(message: GetModuleJobsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.success !== false) {
      writer.uint32(8).bool(message.success);
    }
    for (const v of message.jobs) {
      JobInfo.encode(v!, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetModuleJobsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetModuleJobsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.jobs.push(JobInfo.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetModuleJobsResponse {
    return {
      success: isSet(object.success) ? globalThis.Boolean(object.success) : false,
      jobs: globalThis.Array.isArray(object?.jobs) ? object.jobs.map((e: any) => JobInfo.fromJSON(e)) : [],
    };
  },

  toJSON(message: GetModuleJobsResponse): unknown {
    const obj: any = {};
    if (message.success !== false) {
      obj.success = message.success;
    }
    if (message.jobs?.length) {
      obj.jobs = message.jobs.map((e) => JobInfo.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetModuleJobsResponse>, I>>(base?: I): GetModuleJobsResponse {
    return GetModuleJobsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetModuleJobsResponse>, I>>(object: I): GetModuleJobsResponse {
    const message = createBaseGetModuleJobsResponse();
    message.success = object.success ?? false;
    message.jobs = object.jobs?.map((e) => JobInfo.fromPartial(e)) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
