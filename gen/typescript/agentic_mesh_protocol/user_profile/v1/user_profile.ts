// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.8.3
//   protoc               unknown
// source: agentic_mesh_protocol/user_profile/v1/user_profile.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Struct } from "../../../google/protobuf/struct.js";
import { Timestamp } from "../../../google/protobuf/timestamp.js";

export const protobufPackage = "agentic_mesh_protocol.user_profile.v1";

/** Metadata: Message to represent user metadata (security keys, etc.) */
export interface Metadata {
  /** security_key: Security key for the user */
  securityKey: string;
}

/** Subscription: Message to represent user subscription information */
export interface Subscription {
  /** tier: Subscription tier level */
  tier: string;
  /** status: Current subscription status */
  status: string;
  /** start: Subscription start date */
  start?:
    | Date
    | undefined;
  /** end: Subscription end date */
  end?: Date | undefined;
}

/** Credits: Message to represent user credit allocation and usage */
export interface Credits {
  /** allocated: Total credits allocated to the user */
  allocated: string;
  /** used: Credits used by the user */
  used: string;
  /** timestamp: Last update timestamp for credits */
  timestamp?: Date | undefined;
}

/** UserProfile: Message to represent a user profile */
export interface UserProfile {
  /** user_id: Unique identifier for the user */
  userId: string;
  /** organisation_id: Organisation ID linked to the user */
  organisationId: string;
  /** email: User email address */
  email: string;
  /** first_name: User first name */
  firstName: string;
  /** last_name: User last name */
  lastName: string;
  /** creation_date: Profile creation date */
  creationDate?:
    | Date
    | undefined;
  /** update_date: Profile last update date */
  updateDate?:
    | Date
    | undefined;
  /** locale: User locale preference */
  locale: string;
  /** subscription: User subscription information */
  subscription?:
    | Subscription
    | undefined;
  /** credits: User credit allocation and usage */
  credits?:
    | Credits
    | undefined;
  /** metadata: Additional user metadata (preferences, settings, etc.) */
  metadata?: { [key: string]: any } | undefined;
}

/** GetUserProfileRequest: Request to get a user profile */
export interface GetUserProfileRequest {
  /** user_id: Unique identifier for the user */
  userId: string;
}

/** GetUserProfileResponse: Response after getting a user profile */
export interface GetUserProfileResponse {
  /** success: Success of the operation */
  success: boolean;
  /** user_profile: Retrieved user profile */
  userProfile?: UserProfile | undefined;
}

function createBaseMetadata(): Metadata {
  return { securityKey: "" };
}

export const Metadata: MessageFns<Metadata> = {
  encode(message: Metadata, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.securityKey !== "") {
      writer.uint32(10).string(message.securityKey);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Metadata {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMetadata();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.securityKey = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Metadata {
    return { securityKey: isSet(object.security_key) ? globalThis.String(object.security_key) : "" };
  },

  toJSON(message: Metadata): unknown {
    const obj: any = {};
    if (message.securityKey !== "") {
      obj.security_key = message.securityKey;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Metadata>, I>>(base?: I): Metadata {
    return Metadata.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Metadata>, I>>(object: I): Metadata {
    const message = createBaseMetadata();
    message.securityKey = object.securityKey ?? "";
    return message;
  },
};

function createBaseSubscription(): Subscription {
  return { tier: "", status: "", start: undefined, end: undefined };
}

export const Subscription: MessageFns<Subscription> = {
  encode(message: Subscription, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.tier !== "") {
      writer.uint32(10).string(message.tier);
    }
    if (message.status !== "") {
      writer.uint32(18).string(message.status);
    }
    if (message.start !== undefined) {
      Timestamp.encode(toTimestamp(message.start), writer.uint32(26).fork()).join();
    }
    if (message.end !== undefined) {
      Timestamp.encode(toTimestamp(message.end), writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Subscription {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSubscription();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.tier = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.status = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.start = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.end = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Subscription {
    return {
      tier: isSet(object.tier) ? globalThis.String(object.tier) : "",
      status: isSet(object.status) ? globalThis.String(object.status) : "",
      start: isSet(object.start) ? fromJsonTimestamp(object.start) : undefined,
      end: isSet(object.end) ? fromJsonTimestamp(object.end) : undefined,
    };
  },

  toJSON(message: Subscription): unknown {
    const obj: any = {};
    if (message.tier !== "") {
      obj.tier = message.tier;
    }
    if (message.status !== "") {
      obj.status = message.status;
    }
    if (message.start !== undefined) {
      obj.start = message.start.toISOString();
    }
    if (message.end !== undefined) {
      obj.end = message.end.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Subscription>, I>>(base?: I): Subscription {
    return Subscription.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Subscription>, I>>(object: I): Subscription {
    const message = createBaseSubscription();
    message.tier = object.tier ?? "";
    message.status = object.status ?? "";
    message.start = object.start ?? undefined;
    message.end = object.end ?? undefined;
    return message;
  },
};

function createBaseCredits(): Credits {
  return { allocated: "0", used: "0", timestamp: undefined };
}

export const Credits: MessageFns<Credits> = {
  encode(message: Credits, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.allocated !== "0") {
      writer.uint32(8).int64(message.allocated);
    }
    if (message.used !== "0") {
      writer.uint32(16).int64(message.used);
    }
    if (message.timestamp !== undefined) {
      Timestamp.encode(toTimestamp(message.timestamp), writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Credits {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCredits();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.allocated = reader.int64().toString();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.used = reader.int64().toString();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.timestamp = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Credits {
    return {
      allocated: isSet(object.allocated) ? globalThis.String(object.allocated) : "0",
      used: isSet(object.used) ? globalThis.String(object.used) : "0",
      timestamp: isSet(object.timestamp) ? fromJsonTimestamp(object.timestamp) : undefined,
    };
  },

  toJSON(message: Credits): unknown {
    const obj: any = {};
    if (message.allocated !== "0") {
      obj.allocated = message.allocated;
    }
    if (message.used !== "0") {
      obj.used = message.used;
    }
    if (message.timestamp !== undefined) {
      obj.timestamp = message.timestamp.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Credits>, I>>(base?: I): Credits {
    return Credits.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Credits>, I>>(object: I): Credits {
    const message = createBaseCredits();
    message.allocated = object.allocated ?? "0";
    message.used = object.used ?? "0";
    message.timestamp = object.timestamp ?? undefined;
    return message;
  },
};

function createBaseUserProfile(): UserProfile {
  return {
    userId: "",
    organisationId: "",
    email: "",
    firstName: "",
    lastName: "",
    creationDate: undefined,
    updateDate: undefined,
    locale: "",
    subscription: undefined,
    credits: undefined,
    metadata: undefined,
  };
}

export const UserProfile: MessageFns<UserProfile> = {
  encode(message: UserProfile, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.userId !== "") {
      writer.uint32(10).string(message.userId);
    }
    if (message.organisationId !== "") {
      writer.uint32(18).string(message.organisationId);
    }
    if (message.email !== "") {
      writer.uint32(26).string(message.email);
    }
    if (message.firstName !== "") {
      writer.uint32(34).string(message.firstName);
    }
    if (message.lastName !== "") {
      writer.uint32(42).string(message.lastName);
    }
    if (message.creationDate !== undefined) {
      Timestamp.encode(toTimestamp(message.creationDate), writer.uint32(50).fork()).join();
    }
    if (message.updateDate !== undefined) {
      Timestamp.encode(toTimestamp(message.updateDate), writer.uint32(58).fork()).join();
    }
    if (message.locale !== "") {
      writer.uint32(66).string(message.locale);
    }
    if (message.subscription !== undefined) {
      Subscription.encode(message.subscription, writer.uint32(74).fork()).join();
    }
    if (message.credits !== undefined) {
      Credits.encode(message.credits, writer.uint32(82).fork()).join();
    }
    if (message.metadata !== undefined) {
      Struct.encode(Struct.wrap(message.metadata), writer.uint32(90).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UserProfile {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUserProfile();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.userId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.organisationId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.email = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.firstName = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.lastName = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.creationDate = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.updateDate = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.locale = reader.string();
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.subscription = Subscription.decode(reader, reader.uint32());
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.credits = Credits.decode(reader, reader.uint32());
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.metadata = Struct.unwrap(Struct.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UserProfile {
    return {
      userId: isSet(object.user_id) ? globalThis.String(object.user_id) : "",
      organisationId: isSet(object.organisation_id) ? globalThis.String(object.organisation_id) : "",
      email: isSet(object.email) ? globalThis.String(object.email) : "",
      firstName: isSet(object.first_name) ? globalThis.String(object.first_name) : "",
      lastName: isSet(object.last_name) ? globalThis.String(object.last_name) : "",
      creationDate: isSet(object.creation_date) ? fromJsonTimestamp(object.creation_date) : undefined,
      updateDate: isSet(object.update_date) ? fromJsonTimestamp(object.update_date) : undefined,
      locale: isSet(object.locale) ? globalThis.String(object.locale) : "",
      subscription: isSet(object.subscription) ? Subscription.fromJSON(object.subscription) : undefined,
      credits: isSet(object.credits) ? Credits.fromJSON(object.credits) : undefined,
      metadata: isObject(object.metadata) ? object.metadata : undefined,
    };
  },

  toJSON(message: UserProfile): unknown {
    const obj: any = {};
    if (message.userId !== "") {
      obj.user_id = message.userId;
    }
    if (message.organisationId !== "") {
      obj.organisation_id = message.organisationId;
    }
    if (message.email !== "") {
      obj.email = message.email;
    }
    if (message.firstName !== "") {
      obj.first_name = message.firstName;
    }
    if (message.lastName !== "") {
      obj.last_name = message.lastName;
    }
    if (message.creationDate !== undefined) {
      obj.creation_date = message.creationDate.toISOString();
    }
    if (message.updateDate !== undefined) {
      obj.update_date = message.updateDate.toISOString();
    }
    if (message.locale !== "") {
      obj.locale = message.locale;
    }
    if (message.subscription !== undefined) {
      obj.subscription = Subscription.toJSON(message.subscription);
    }
    if (message.credits !== undefined) {
      obj.credits = Credits.toJSON(message.credits);
    }
    if (message.metadata !== undefined) {
      obj.metadata = message.metadata;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UserProfile>, I>>(base?: I): UserProfile {
    return UserProfile.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UserProfile>, I>>(object: I): UserProfile {
    const message = createBaseUserProfile();
    message.userId = object.userId ?? "";
    message.organisationId = object.organisationId ?? "";
    message.email = object.email ?? "";
    message.firstName = object.firstName ?? "";
    message.lastName = object.lastName ?? "";
    message.creationDate = object.creationDate ?? undefined;
    message.updateDate = object.updateDate ?? undefined;
    message.locale = object.locale ?? "";
    message.subscription = (object.subscription !== undefined && object.subscription !== null)
      ? Subscription.fromPartial(object.subscription)
      : undefined;
    message.credits = (object.credits !== undefined && object.credits !== null)
      ? Credits.fromPartial(object.credits)
      : undefined;
    message.metadata = object.metadata ?? undefined;
    return message;
  },
};

function createBaseGetUserProfileRequest(): GetUserProfileRequest {
  return { userId: "" };
}

export const GetUserProfileRequest: MessageFns<GetUserProfileRequest> = {
  encode(message: GetUserProfileRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.userId !== "") {
      writer.uint32(10).string(message.userId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetUserProfileRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetUserProfileRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.userId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetUserProfileRequest {
    return { userId: isSet(object.user_id) ? globalThis.String(object.user_id) : "" };
  },

  toJSON(message: GetUserProfileRequest): unknown {
    const obj: any = {};
    if (message.userId !== "") {
      obj.user_id = message.userId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetUserProfileRequest>, I>>(base?: I): GetUserProfileRequest {
    return GetUserProfileRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetUserProfileRequest>, I>>(object: I): GetUserProfileRequest {
    const message = createBaseGetUserProfileRequest();
    message.userId = object.userId ?? "";
    return message;
  },
};

function createBaseGetUserProfileResponse(): GetUserProfileResponse {
  return { success: false, userProfile: undefined };
}

export const GetUserProfileResponse: MessageFns<GetUserProfileResponse> = {
  encode(message: GetUserProfileResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.success !== false) {
      writer.uint32(8).bool(message.success);
    }
    if (message.userProfile !== undefined) {
      UserProfile.encode(message.userProfile, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetUserProfileResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetUserProfileResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.userProfile = UserProfile.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetUserProfileResponse {
    return {
      success: isSet(object.success) ? globalThis.Boolean(object.success) : false,
      userProfile: isSet(object.user_profile) ? UserProfile.fromJSON(object.user_profile) : undefined,
    };
  },

  toJSON(message: GetUserProfileResponse): unknown {
    const obj: any = {};
    if (message.success !== false) {
      obj.success = message.success;
    }
    if (message.userProfile !== undefined) {
      obj.user_profile = UserProfile.toJSON(message.userProfile);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetUserProfileResponse>, I>>(base?: I): GetUserProfileResponse {
    return GetUserProfileResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetUserProfileResponse>, I>>(object: I): GetUserProfileResponse {
    const message = createBaseGetUserProfileResponse();
    message.success = object.success ?? false;
    message.userProfile = (object.userProfile !== undefined && object.userProfile !== null)
      ? UserProfile.fromPartial(object.userProfile)
      : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000).toString();
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (globalThis.Number(t.seconds) || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
