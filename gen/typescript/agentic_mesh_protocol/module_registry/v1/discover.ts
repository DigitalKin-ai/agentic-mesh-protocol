// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.8.3
//   protoc               unknown
// source: agentic_mesh_protocol/module_registry/v1/discover.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Metadata, Tag } from "./metadata.js";

export const protobufPackage = "agentic_mesh_protocol.module_registry.v1";

/** DiscoverSearchRequest */
export interface DiscoverSearchRequest {
  /** module_type: Type of the module to search for (trigger, tool, kin, view). */
  moduleType?:
    | string
    | undefined;
  /** name: Module's name to search */
  name?:
    | string
    | undefined;
  /** tags: List of tags to filter modules with. */
  tags: Tag[];
  /** description: Module's description to search. */
  description?: string | undefined;
}

/** DiscoverSearchResponse */
export interface DiscoverSearchResponse {
  /** modules: List of matching modules. */
  modules: DiscoverInfoResponse[];
}

/** DiscoverInfoRequest */
export interface DiscoverInfoRequest {
  /** module_id: Id of the module. */
  moduleId: string;
}

/** DiscoverInfoResponse */
export interface DiscoverInfoResponse {
  /** module_id: Id of the module. */
  moduleId: string;
  /** module_type: Type of the module (trigger, tool, kin, view). */
  moduleType: string;
  /** address: Address used to communicate with the module. */
  address: string;
  /** port: Port used to communicate with the module. */
  port: number;
  /** version: Current module version. */
  version: string;
  /** metadata: user defined module name, description and tags */
  metadata?: Metadata | undefined;
}

function createBaseDiscoverSearchRequest(): DiscoverSearchRequest {
  return { moduleType: undefined, name: undefined, tags: [], description: undefined };
}

export const DiscoverSearchRequest: MessageFns<DiscoverSearchRequest> = {
  encode(message: DiscoverSearchRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.moduleType !== undefined) {
      writer.uint32(10).string(message.moduleType);
    }
    if (message.name !== undefined) {
      writer.uint32(18).string(message.name);
    }
    for (const v of message.tags) {
      Tag.encode(v!, writer.uint32(26).fork()).join();
    }
    if (message.description !== undefined) {
      writer.uint32(34).string(message.description);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DiscoverSearchRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDiscoverSearchRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.moduleType = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.tags.push(Tag.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.description = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DiscoverSearchRequest {
    return {
      moduleType: isSet(object.module_type) ? globalThis.String(object.module_type) : undefined,
      name: isSet(object.name) ? globalThis.String(object.name) : undefined,
      tags: globalThis.Array.isArray(object?.tags) ? object.tags.map((e: any) => Tag.fromJSON(e)) : [],
      description: isSet(object.description) ? globalThis.String(object.description) : undefined,
    };
  },

  toJSON(message: DiscoverSearchRequest): unknown {
    const obj: any = {};
    if (message.moduleType !== undefined) {
      obj.module_type = message.moduleType;
    }
    if (message.name !== undefined) {
      obj.name = message.name;
    }
    if (message.tags?.length) {
      obj.tags = message.tags.map((e) => Tag.toJSON(e));
    }
    if (message.description !== undefined) {
      obj.description = message.description;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DiscoverSearchRequest>, I>>(base?: I): DiscoverSearchRequest {
    return DiscoverSearchRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DiscoverSearchRequest>, I>>(object: I): DiscoverSearchRequest {
    const message = createBaseDiscoverSearchRequest();
    message.moduleType = object.moduleType ?? undefined;
    message.name = object.name ?? undefined;
    message.tags = object.tags?.map((e) => Tag.fromPartial(e)) || [];
    message.description = object.description ?? undefined;
    return message;
  },
};

function createBaseDiscoverSearchResponse(): DiscoverSearchResponse {
  return { modules: [] };
}

export const DiscoverSearchResponse: MessageFns<DiscoverSearchResponse> = {
  encode(message: DiscoverSearchResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.modules) {
      DiscoverInfoResponse.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DiscoverSearchResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDiscoverSearchResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.modules.push(DiscoverInfoResponse.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DiscoverSearchResponse {
    return {
      modules: globalThis.Array.isArray(object?.modules)
        ? object.modules.map((e: any) => DiscoverInfoResponse.fromJSON(e))
        : [],
    };
  },

  toJSON(message: DiscoverSearchResponse): unknown {
    const obj: any = {};
    if (message.modules?.length) {
      obj.modules = message.modules.map((e) => DiscoverInfoResponse.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DiscoverSearchResponse>, I>>(base?: I): DiscoverSearchResponse {
    return DiscoverSearchResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DiscoverSearchResponse>, I>>(object: I): DiscoverSearchResponse {
    const message = createBaseDiscoverSearchResponse();
    message.modules = object.modules?.map((e) => DiscoverInfoResponse.fromPartial(e)) || [];
    return message;
  },
};

function createBaseDiscoverInfoRequest(): DiscoverInfoRequest {
  return { moduleId: "" };
}

export const DiscoverInfoRequest: MessageFns<DiscoverInfoRequest> = {
  encode(message: DiscoverInfoRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.moduleId !== "") {
      writer.uint32(10).string(message.moduleId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DiscoverInfoRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDiscoverInfoRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.moduleId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DiscoverInfoRequest {
    return { moduleId: isSet(object.module_id) ? globalThis.String(object.module_id) : "" };
  },

  toJSON(message: DiscoverInfoRequest): unknown {
    const obj: any = {};
    if (message.moduleId !== "") {
      obj.module_id = message.moduleId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DiscoverInfoRequest>, I>>(base?: I): DiscoverInfoRequest {
    return DiscoverInfoRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DiscoverInfoRequest>, I>>(object: I): DiscoverInfoRequest {
    const message = createBaseDiscoverInfoRequest();
    message.moduleId = object.moduleId ?? "";
    return message;
  },
};

function createBaseDiscoverInfoResponse(): DiscoverInfoResponse {
  return { moduleId: "", moduleType: "", address: "", port: 0, version: "", metadata: undefined };
}

export const DiscoverInfoResponse: MessageFns<DiscoverInfoResponse> = {
  encode(message: DiscoverInfoResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.moduleId !== "") {
      writer.uint32(10).string(message.moduleId);
    }
    if (message.moduleType !== "") {
      writer.uint32(18).string(message.moduleType);
    }
    if (message.address !== "") {
      writer.uint32(26).string(message.address);
    }
    if (message.port !== 0) {
      writer.uint32(32).int32(message.port);
    }
    if (message.version !== "") {
      writer.uint32(42).string(message.version);
    }
    if (message.metadata !== undefined) {
      Metadata.encode(message.metadata, writer.uint32(50).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DiscoverInfoResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDiscoverInfoResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.moduleId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.moduleType = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.address = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.port = reader.int32();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.version = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.metadata = Metadata.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DiscoverInfoResponse {
    return {
      moduleId: isSet(object.module_id) ? globalThis.String(object.module_id) : "",
      moduleType: isSet(object.module_type) ? globalThis.String(object.module_type) : "",
      address: isSet(object.address) ? globalThis.String(object.address) : "",
      port: isSet(object.port) ? globalThis.Number(object.port) : 0,
      version: isSet(object.version) ? globalThis.String(object.version) : "",
      metadata: isSet(object.metadata) ? Metadata.fromJSON(object.metadata) : undefined,
    };
  },

  toJSON(message: DiscoverInfoResponse): unknown {
    const obj: any = {};
    if (message.moduleId !== "") {
      obj.module_id = message.moduleId;
    }
    if (message.moduleType !== "") {
      obj.module_type = message.moduleType;
    }
    if (message.address !== "") {
      obj.address = message.address;
    }
    if (message.port !== 0) {
      obj.port = Math.round(message.port);
    }
    if (message.version !== "") {
      obj.version = message.version;
    }
    if (message.metadata !== undefined) {
      obj.metadata = Metadata.toJSON(message.metadata);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DiscoverInfoResponse>, I>>(base?: I): DiscoverInfoResponse {
    return DiscoverInfoResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DiscoverInfoResponse>, I>>(object: I): DiscoverInfoResponse {
    const message = createBaseDiscoverInfoResponse();
    message.moduleId = object.moduleId ?? "";
    message.moduleType = object.moduleType ?? "";
    message.address = object.address ?? "";
    message.port = object.port ?? 0;
    message.version = object.version ?? "";
    message.metadata = (object.metadata !== undefined && object.metadata !== null)
      ? Metadata.fromPartial(object.metadata)
      : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
