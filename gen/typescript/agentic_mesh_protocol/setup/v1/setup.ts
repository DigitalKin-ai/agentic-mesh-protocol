// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.8.3
//   protoc               unknown
// source: agentic_mesh_protocol/setup/v1/setup.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Struct } from "../../../google/protobuf/struct.js";
import { Timestamp } from "../../../google/protobuf/timestamp.js";

export const protobufPackage = "agentic_mesh_protocol.setup.v1";

/**
 * SetupStatus
 * Represents the status of a setup.
 *
 * Fields:
 * - DRAFT: Setup is a draft
 * - VALIDATING: Setup is being validated
 * - READY: Setup is ready
 * - PAUSED: Setup is paused
 * - FAILED: Setup failed
 * - ARCHIVED: Setup has been archived
 * - NEEDS_CONFIGURATION: Setup needs configuration
 * - CONFIGURATION_FAILED: Setup configuration failed
 * - CONFIGURATION_SUCCEEDED: Setup configuration succeeded
 */
export enum SetupStatus {
  /** DRAFT - DRAFT: Setup is a draft */
  DRAFT = 0,
  /** VALIDATING - VALIDATING: Setup is being validated */
  VALIDATING = 1,
  /** READY - READY: Setup is ready */
  READY = 2,
  /** PAUSED - PAUSED: Setup is paused */
  PAUSED = 4,
  /** FAILED - FAILED: Setup failed */
  FAILED = 5,
  /** ARCHIVED - ARCHIVED: Setup has been archived */
  ARCHIVED = 6,
  /** NEEDS_CONFIGURATION - NEEDS_CONFIGURATION: Setup needs configuration */
  NEEDS_CONFIGURATION = 7,
  /** CONFIGURATION_FAILED - CONFIGURATION_FAILED: Setup configuration failed */
  CONFIGURATION_FAILED = 8,
  /** CONFIGURATION_SUCCEEDED - CONFIGURATION_SUCCEEDED: Setup configuration succeeded */
  CONFIGURATION_SUCCEEDED = 9,
  UNRECOGNIZED = -1,
}

export function setupStatusFromJSON(object: any): SetupStatus {
  switch (object) {
    case 0:
    case "DRAFT":
      return SetupStatus.DRAFT;
    case 1:
    case "VALIDATING":
      return SetupStatus.VALIDATING;
    case 2:
    case "READY":
      return SetupStatus.READY;
    case 4:
    case "PAUSED":
      return SetupStatus.PAUSED;
    case 5:
    case "FAILED":
      return SetupStatus.FAILED;
    case 6:
    case "ARCHIVED":
      return SetupStatus.ARCHIVED;
    case 7:
    case "NEEDS_CONFIGURATION":
      return SetupStatus.NEEDS_CONFIGURATION;
    case 8:
    case "CONFIGURATION_FAILED":
      return SetupStatus.CONFIGURATION_FAILED;
    case 9:
    case "CONFIGURATION_SUCCEEDED":
      return SetupStatus.CONFIGURATION_SUCCEEDED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return SetupStatus.UNRECOGNIZED;
  }
}

export function setupStatusToJSON(object: SetupStatus): string {
  switch (object) {
    case SetupStatus.DRAFT:
      return "DRAFT";
    case SetupStatus.VALIDATING:
      return "VALIDATING";
    case SetupStatus.READY:
      return "READY";
    case SetupStatus.PAUSED:
      return "PAUSED";
    case SetupStatus.FAILED:
      return "FAILED";
    case SetupStatus.ARCHIVED:
      return "ARCHIVED";
    case SetupStatus.NEEDS_CONFIGURATION:
      return "NEEDS_CONFIGURATION";
    case SetupStatus.CONFIGURATION_FAILED:
      return "CONFIGURATION_FAILED";
    case SetupStatus.CONFIGURATION_SUCCEEDED:
      return "CONFIGURATION_SUCCEEDED";
    case SetupStatus.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/**
 * SetupVersion
 * Represents a version of a setup's configuration.
 *
 * Fields:
 * - id: The unique identifier of the setup version.
 * - setup_id: The unique identifier of the parent setup.
 * - name: The identifier of the parent setup.
 * - version: A label for the version (e.g., "v1", "v2").
 * - content: Key-value pairs containing configuration content.
 * - creation_date: setup version creation date
 */
export interface SetupVersion {
  /** id: The unique identifier of the setup version. */
  id: string;
  /** setup_id: The unique identifier of the parent setup. */
  setupId: string;
  /** version: The label for this version. */
  version: string;
  /** content: Key-value pairs for configuration content. */
  content?:
    | { [key: string]: any }
    | undefined;
  /** creation_date: setup version creation date */
  creationDate?: Date | undefined;
}

/**
 * Setup
 * Represents a configuration associated with a mission.
 *
 * Fields:
 * - id: unique id used in database
 * - name: The name of the setup.
 * - organisation_id: The unique identifier of the organisation.
 * - owner_id: The owner_id of the setup.
 * - module_id: The module identifier associated with the setup.
 * - current_setup_version: The identifier of the active setup version.
 */
export interface Setup {
  /** id: The unique identifier of the setup. */
  id: string;
  /** name: The name of the setup. */
  name: string;
  /** organisation_id: The unique identifier of the organisation. */
  organisationId: string;
  /** owner_id: The owner_id of the setup. */
  ownerId: string;
  /** module_id: The module identifier associated with the setup. */
  moduleId: string;
  /** current_setup_version: The unique identifier of the active setup version. */
  currentSetupVersion?:
    | SetupVersion
    | undefined;
  /** status: The status of the setup. */
  status: SetupStatus;
}

/**
 * CreateSetupRequest
 * This request is used to create a new setup.
 *
 * Fields:
 * - name: The name of the setup.
 * - organisation_id: The unique identifier of the organisation.
 * - owner_id: The owner_id of the setup.
 * - module_id: The module identifier associated with the setup.
 * - current_setup_version: The unique identifier of the active setup version.
 */
export interface CreateSetupRequest {
  /** name: The name of the setup. */
  name: string;
  /** organisation_id: The unique identifier of the organisation. */
  organisationId: string;
  /** owner_id: The owner_id of the setup. */
  ownerId: string;
  /** module_id: The module identifier associated with the setup. */
  moduleId: string;
  /** current_setup_version: The unique identifier of the active setup version. */
  currentSetupVersion?:
    | SetupVersion
    | undefined;
  /** status: The status of the setup. */
  status: SetupStatus;
}

/**
 * CreateSetupResponse
 * Returns the newly created setup status flag.
 *
 * Fields:
 * - success: description flag of the operation.
 * - setup: The newly created setup entity.
 */
export interface CreateSetupResponse {
  /** success: description flag of the operation. */
  success: boolean;
  /** setup: The newly created setup entity. */
  setup?: Setup | undefined;
}

/**
 * GetSetupRequest
 * This request is used to retrieve a setup by its unique identifier.
 *
 * Fields:
 * - setup_id: The unique identifier of the setup.
 * - version: (Optional) The unique identifier of the active setup version.
 */
export interface GetSetupRequest {
  /** setup_id: The unique identifier of the setup. */
  setupId: string;
  /** version: The unique identifier of the active setup version. */
  version?: string | undefined;
}

/**
 * GetSetupResponse
 * Returns the setup corresponding to the requested identifier.
 *
 * Fields:
 * - setup: The retrieved setup entity.
 * - setup_version: The retrieved setup_version if exists.
 */
export interface GetSetupResponse {
  /** setup: The retrieved setup entity. */
  setup?:
    | Setup
    | undefined;
  /** setup_version: The retrieved setup_version if exists. */
  setupVersion?: SetupVersion | undefined;
}

/**
 * UpdateSetupRequest
 * This request is used to update an existing setup.
 *
 * Fields:
 * - name: The unique identifier of the setup to update.
 * - owner_id: (Optional) The new owner_id.
 * - current_setup_version: (Optional) The new active setup version identifier.
 */
export interface UpdateSetupRequest {
  /** setup_id: The unique identifier of the setup. */
  setupId: string;
  /** name: The unique identifier of the setup to update. */
  name: string;
  /** owner_id: The owner_id of the setup. */
  ownerId: string;
  /** current_setup_version: The unique identifier of the active setup version. */
  currentSetupVersion?:
    | SetupVersion
    | undefined;
  /** status: The status of the setup. */
  status: SetupStatus;
}

/**
 * UpdateSetupResponse
 * Returns the updated setup status flag.
 *
 * Fields:
 * - success: description flag of the operation.
 * - setup: The updated setup entity.
 */
export interface UpdateSetupResponse {
  /** success: description flag of the operation. */
  success: boolean;
  /** setup: The updated setup entity. */
  setup?: Setup | undefined;
}

/**
 * DeleteSetupRequest
 * This request is used to delete a setup by its unique identifier.
 *
 * Fields:
 * - setup_id: The unique identifier of the setup to delete.
 */
export interface DeleteSetupRequest {
  /** setup_id: The unique identifier of the setup. */
  setupId: string;
}

/**
 * DeleteSetupResponse
 * Indicates that the setup has been successfully deleted.
 */
export interface DeleteSetupResponse {
  /** success: description flag of the operation. */
  success: boolean;
}

/**
 * CreateSetupVersionRequest
 * This request is used to create a new setup version.
 *
 * Fields:
 * - setup_id: The unique identifier of the parent setup.
 * - version: The label for the new version.
 * - content: The configuration content for this version.
 */
export interface CreateSetupVersionRequest {
  /** setup_id: The unique identifier of the parent setup. */
  setupId: string;
  /** version: The version label (e.g., "v1", "v2"). */
  version: string;
  /** content: Key-value pairs for configuration content. */
  content?: { [key: string]: any } | undefined;
}

/**
 * CreateSetupVersionResponse
 * Returns the newly created setup version status flag.
 *
 * Fields:
 * - success: description flag of the operation.
 * - setup_version: The newly created setup version entity.
 */
export interface CreateSetupVersionResponse {
  /** success: description flag of the operation. */
  success: boolean;
  /** setup_version: The newly created setup version entity. */
  setupVersion?: SetupVersion | undefined;
}

/**
 * GetSetupVersionRequest
 * This request is used to retrieve a setup version by its unique identifier.
 *
 * Fields:
 * - setup_version_id: The unique identifier of the setup version.
 */
export interface GetSetupVersionRequest {
  /** setup_version_id: The unique identifier of the setup version. */
  setupVersionId: string;
}

/**
 * GetSetupVersionResponse
 * Returns the setup version corresponding to the setup_version_id.
 *
 * Fields:
 * - setup_version: The retrieved setup version entity.
 */
export interface GetSetupVersionResponse {
  /** setup_version: The retrieved setup version entity. */
  setupVersion?: SetupVersion | undefined;
}

/**
 * SearchSetupVersionsRequest
 * This request is used to search for setup versions using filters.
 *
 * Fields:
 * - setup_id: (Optional) Filter by the parent setup identifier.
 * - version: (Optional) Filter by the version label.
 */
export interface SearchSetupVersionsRequest {
  /** setup_id: The unique identifier of the parent setup. */
  setupId: string;
  /** version: The version label to filter wirh. */
  version?: string | undefined;
}

/**
 * SearchSetupVersionsResponse
 * Returns a list of setup versions that match the search criteria.
 */
export interface SearchSetupVersionsResponse {
  /** setup_versions: A list of setup versions matching the criteria. */
  setupVersions: SetupVersion[];
}

/**
 * UpdateSetupVersionRequest
 * This request is used to update an existing setup version.
 *
 * Fields:
 * - setup_version_id: The unique identifier of the setup version to update.
 * - version: (Optional) The new version label.
 * - content: (Optional) The new configuration content.
 */
export interface UpdateSetupVersionRequest {
  /** setup_version_id: The unique identifier of the setup version to update. */
  setupVersionId: string;
  /** version: The version label. */
  version: string;
  /** content: Key-value pairs for configuration content. */
  content?: { [key: string]: any } | undefined;
}

/**
 * UpdateSetupVersionResponse
 * Returns the updated setup version.
 *
 * Fields:
 * - success: description flag of the operation.
 * - setup_version: The updated setup version entity.
 */
export interface UpdateSetupVersionResponse {
  /** success: description flag of the operation. */
  success: boolean;
  /** setup_version: The updated setup version entity. */
  setupVersion?: SetupVersion | undefined;
}

/**
 * DeleteSetupVersionRequest
 * This request is used to delete a setup version by its unique identifier.
 *
 * Fields:
 * - setup_version_id: The unique identifier of the setup version to delete.
 */
export interface DeleteSetupVersionRequest {
  /** setup_version_id: The unique identifier of the setup version. */
  setupVersionId: string;
}

/**
 * DeleteSetupVersionResponse
 * Indicates that the setup version has been successfully deleted.
 */
export interface DeleteSetupVersionResponse {
  /** success: description flag of the operation. */
  success: boolean;
}

/** ListSetupsRequest is the request message for listing setups. */
export interface ListSetupsRequest {
  /** organisation_id: Filter by organisation ID. */
  organisationId?:
    | string
    | undefined;
  /** owner_id: Filter by owner ID. */
  ownerId?:
    | string
    | undefined;
  /** limit: Maximum number of setups to return. */
  limit: number;
  /** offset: Number of setups to skip. */
  offset: number;
}

/** ListSetupsResponse is the response message containing a list of setups. */
export interface ListSetupsResponse {
  /** setups: List of setups matching the criteria. */
  setups: Setup[];
  /** total_count: Total number of setups matching the criteria. */
  totalCount: number;
}

function createBaseSetupVersion(): SetupVersion {
  return { id: "", setupId: "", version: "", content: undefined, creationDate: undefined };
}

export const SetupVersion: MessageFns<SetupVersion> = {
  encode(message: SetupVersion, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.setupId !== "") {
      writer.uint32(18).string(message.setupId);
    }
    if (message.version !== "") {
      writer.uint32(26).string(message.version);
    }
    if (message.content !== undefined) {
      Struct.encode(Struct.wrap(message.content), writer.uint32(34).fork()).join();
    }
    if (message.creationDate !== undefined) {
      Timestamp.encode(toTimestamp(message.creationDate), writer.uint32(42).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SetupVersion {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSetupVersion();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.setupId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.version = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.content = Struct.unwrap(Struct.decode(reader, reader.uint32()));
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.creationDate = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SetupVersion {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      setupId: isSet(object.setup_id) ? globalThis.String(object.setup_id) : "",
      version: isSet(object.version) ? globalThis.String(object.version) : "",
      content: isObject(object.content) ? object.content : undefined,
      creationDate: isSet(object.creation_date) ? fromJsonTimestamp(object.creation_date) : undefined,
    };
  },

  toJSON(message: SetupVersion): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.setupId !== "") {
      obj.setup_id = message.setupId;
    }
    if (message.version !== "") {
      obj.version = message.version;
    }
    if (message.content !== undefined) {
      obj.content = message.content;
    }
    if (message.creationDate !== undefined) {
      obj.creation_date = message.creationDate.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SetupVersion>, I>>(base?: I): SetupVersion {
    return SetupVersion.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SetupVersion>, I>>(object: I): SetupVersion {
    const message = createBaseSetupVersion();
    message.id = object.id ?? "";
    message.setupId = object.setupId ?? "";
    message.version = object.version ?? "";
    message.content = object.content ?? undefined;
    message.creationDate = object.creationDate ?? undefined;
    return message;
  },
};

function createBaseSetup(): Setup {
  return { id: "", name: "", organisationId: "", ownerId: "", moduleId: "", currentSetupVersion: undefined, status: 0 };
}

export const Setup: MessageFns<Setup> = {
  encode(message: Setup, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.organisationId !== "") {
      writer.uint32(26).string(message.organisationId);
    }
    if (message.ownerId !== "") {
      writer.uint32(34).string(message.ownerId);
    }
    if (message.moduleId !== "") {
      writer.uint32(42).string(message.moduleId);
    }
    if (message.currentSetupVersion !== undefined) {
      SetupVersion.encode(message.currentSetupVersion, writer.uint32(50).fork()).join();
    }
    if (message.status !== 0) {
      writer.uint32(56).int32(message.status);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Setup {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSetup();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.organisationId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.ownerId = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.moduleId = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.currentSetupVersion = SetupVersion.decode(reader, reader.uint32());
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Setup {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      organisationId: isSet(object.organisation_id) ? globalThis.String(object.organisation_id) : "",
      ownerId: isSet(object.owner_id) ? globalThis.String(object.owner_id) : "",
      moduleId: isSet(object.module_id) ? globalThis.String(object.module_id) : "",
      currentSetupVersion: isSet(object.current_setup_version)
        ? SetupVersion.fromJSON(object.current_setup_version)
        : undefined,
      status: isSet(object.status) ? setupStatusFromJSON(object.status) : 0,
    };
  },

  toJSON(message: Setup): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.organisationId !== "") {
      obj.organisation_id = message.organisationId;
    }
    if (message.ownerId !== "") {
      obj.owner_id = message.ownerId;
    }
    if (message.moduleId !== "") {
      obj.module_id = message.moduleId;
    }
    if (message.currentSetupVersion !== undefined) {
      obj.current_setup_version = SetupVersion.toJSON(message.currentSetupVersion);
    }
    if (message.status !== 0) {
      obj.status = setupStatusToJSON(message.status);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Setup>, I>>(base?: I): Setup {
    return Setup.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Setup>, I>>(object: I): Setup {
    const message = createBaseSetup();
    message.id = object.id ?? "";
    message.name = object.name ?? "";
    message.organisationId = object.organisationId ?? "";
    message.ownerId = object.ownerId ?? "";
    message.moduleId = object.moduleId ?? "";
    message.currentSetupVersion = (object.currentSetupVersion !== undefined && object.currentSetupVersion !== null)
      ? SetupVersion.fromPartial(object.currentSetupVersion)
      : undefined;
    message.status = object.status ?? 0;
    return message;
  },
};

function createBaseCreateSetupRequest(): CreateSetupRequest {
  return { name: "", organisationId: "", ownerId: "", moduleId: "", currentSetupVersion: undefined, status: 0 };
}

export const CreateSetupRequest: MessageFns<CreateSetupRequest> = {
  encode(message: CreateSetupRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.organisationId !== "") {
      writer.uint32(18).string(message.organisationId);
    }
    if (message.ownerId !== "") {
      writer.uint32(26).string(message.ownerId);
    }
    if (message.moduleId !== "") {
      writer.uint32(34).string(message.moduleId);
    }
    if (message.currentSetupVersion !== undefined) {
      SetupVersion.encode(message.currentSetupVersion, writer.uint32(42).fork()).join();
    }
    if (message.status !== 0) {
      writer.uint32(48).int32(message.status);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateSetupRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateSetupRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.organisationId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.ownerId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.moduleId = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.currentSetupVersion = SetupVersion.decode(reader, reader.uint32());
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateSetupRequest {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      organisationId: isSet(object.organisation_id) ? globalThis.String(object.organisation_id) : "",
      ownerId: isSet(object.owner_id) ? globalThis.String(object.owner_id) : "",
      moduleId: isSet(object.module_id) ? globalThis.String(object.module_id) : "",
      currentSetupVersion: isSet(object.current_setup_version)
        ? SetupVersion.fromJSON(object.current_setup_version)
        : undefined,
      status: isSet(object.status) ? setupStatusFromJSON(object.status) : 0,
    };
  },

  toJSON(message: CreateSetupRequest): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.organisationId !== "") {
      obj.organisation_id = message.organisationId;
    }
    if (message.ownerId !== "") {
      obj.owner_id = message.ownerId;
    }
    if (message.moduleId !== "") {
      obj.module_id = message.moduleId;
    }
    if (message.currentSetupVersion !== undefined) {
      obj.current_setup_version = SetupVersion.toJSON(message.currentSetupVersion);
    }
    if (message.status !== 0) {
      obj.status = setupStatusToJSON(message.status);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CreateSetupRequest>, I>>(base?: I): CreateSetupRequest {
    return CreateSetupRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CreateSetupRequest>, I>>(object: I): CreateSetupRequest {
    const message = createBaseCreateSetupRequest();
    message.name = object.name ?? "";
    message.organisationId = object.organisationId ?? "";
    message.ownerId = object.ownerId ?? "";
    message.moduleId = object.moduleId ?? "";
    message.currentSetupVersion = (object.currentSetupVersion !== undefined && object.currentSetupVersion !== null)
      ? SetupVersion.fromPartial(object.currentSetupVersion)
      : undefined;
    message.status = object.status ?? 0;
    return message;
  },
};

function createBaseCreateSetupResponse(): CreateSetupResponse {
  return { success: false, setup: undefined };
}

export const CreateSetupResponse: MessageFns<CreateSetupResponse> = {
  encode(message: CreateSetupResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.success !== false) {
      writer.uint32(8).bool(message.success);
    }
    if (message.setup !== undefined) {
      Setup.encode(message.setup, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateSetupResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateSetupResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.setup = Setup.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateSetupResponse {
    return {
      success: isSet(object.success) ? globalThis.Boolean(object.success) : false,
      setup: isSet(object.setup) ? Setup.fromJSON(object.setup) : undefined,
    };
  },

  toJSON(message: CreateSetupResponse): unknown {
    const obj: any = {};
    if (message.success !== false) {
      obj.success = message.success;
    }
    if (message.setup !== undefined) {
      obj.setup = Setup.toJSON(message.setup);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CreateSetupResponse>, I>>(base?: I): CreateSetupResponse {
    return CreateSetupResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CreateSetupResponse>, I>>(object: I): CreateSetupResponse {
    const message = createBaseCreateSetupResponse();
    message.success = object.success ?? false;
    message.setup = (object.setup !== undefined && object.setup !== null) ? Setup.fromPartial(object.setup) : undefined;
    return message;
  },
};

function createBaseGetSetupRequest(): GetSetupRequest {
  return { setupId: "", version: undefined };
}

export const GetSetupRequest: MessageFns<GetSetupRequest> = {
  encode(message: GetSetupRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.setupId !== "") {
      writer.uint32(10).string(message.setupId);
    }
    if (message.version !== undefined) {
      writer.uint32(18).string(message.version);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetSetupRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetSetupRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.setupId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.version = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetSetupRequest {
    return {
      setupId: isSet(object.setup_id) ? globalThis.String(object.setup_id) : "",
      version: isSet(object.version) ? globalThis.String(object.version) : undefined,
    };
  },

  toJSON(message: GetSetupRequest): unknown {
    const obj: any = {};
    if (message.setupId !== "") {
      obj.setup_id = message.setupId;
    }
    if (message.version !== undefined) {
      obj.version = message.version;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetSetupRequest>, I>>(base?: I): GetSetupRequest {
    return GetSetupRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetSetupRequest>, I>>(object: I): GetSetupRequest {
    const message = createBaseGetSetupRequest();
    message.setupId = object.setupId ?? "";
    message.version = object.version ?? undefined;
    return message;
  },
};

function createBaseGetSetupResponse(): GetSetupResponse {
  return { setup: undefined, setupVersion: undefined };
}

export const GetSetupResponse: MessageFns<GetSetupResponse> = {
  encode(message: GetSetupResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.setup !== undefined) {
      Setup.encode(message.setup, writer.uint32(10).fork()).join();
    }
    if (message.setupVersion !== undefined) {
      SetupVersion.encode(message.setupVersion, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetSetupResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetSetupResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.setup = Setup.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.setupVersion = SetupVersion.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetSetupResponse {
    return {
      setup: isSet(object.setup) ? Setup.fromJSON(object.setup) : undefined,
      setupVersion: isSet(object.setup_version) ? SetupVersion.fromJSON(object.setup_version) : undefined,
    };
  },

  toJSON(message: GetSetupResponse): unknown {
    const obj: any = {};
    if (message.setup !== undefined) {
      obj.setup = Setup.toJSON(message.setup);
    }
    if (message.setupVersion !== undefined) {
      obj.setup_version = SetupVersion.toJSON(message.setupVersion);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetSetupResponse>, I>>(base?: I): GetSetupResponse {
    return GetSetupResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetSetupResponse>, I>>(object: I): GetSetupResponse {
    const message = createBaseGetSetupResponse();
    message.setup = (object.setup !== undefined && object.setup !== null) ? Setup.fromPartial(object.setup) : undefined;
    message.setupVersion = (object.setupVersion !== undefined && object.setupVersion !== null)
      ? SetupVersion.fromPartial(object.setupVersion)
      : undefined;
    return message;
  },
};

function createBaseUpdateSetupRequest(): UpdateSetupRequest {
  return { setupId: "", name: "", ownerId: "", currentSetupVersion: undefined, status: 0 };
}

export const UpdateSetupRequest: MessageFns<UpdateSetupRequest> = {
  encode(message: UpdateSetupRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.setupId !== "") {
      writer.uint32(10).string(message.setupId);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.ownerId !== "") {
      writer.uint32(26).string(message.ownerId);
    }
    if (message.currentSetupVersion !== undefined) {
      SetupVersion.encode(message.currentSetupVersion, writer.uint32(34).fork()).join();
    }
    if (message.status !== 0) {
      writer.uint32(40).int32(message.status);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UpdateSetupRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpdateSetupRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.setupId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.ownerId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.currentSetupVersion = SetupVersion.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UpdateSetupRequest {
    return {
      setupId: isSet(object.setup_id) ? globalThis.String(object.setup_id) : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      ownerId: isSet(object.owner_id) ? globalThis.String(object.owner_id) : "",
      currentSetupVersion: isSet(object.current_setup_version)
        ? SetupVersion.fromJSON(object.current_setup_version)
        : undefined,
      status: isSet(object.status) ? setupStatusFromJSON(object.status) : 0,
    };
  },

  toJSON(message: UpdateSetupRequest): unknown {
    const obj: any = {};
    if (message.setupId !== "") {
      obj.setup_id = message.setupId;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.ownerId !== "") {
      obj.owner_id = message.ownerId;
    }
    if (message.currentSetupVersion !== undefined) {
      obj.current_setup_version = SetupVersion.toJSON(message.currentSetupVersion);
    }
    if (message.status !== 0) {
      obj.status = setupStatusToJSON(message.status);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UpdateSetupRequest>, I>>(base?: I): UpdateSetupRequest {
    return UpdateSetupRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UpdateSetupRequest>, I>>(object: I): UpdateSetupRequest {
    const message = createBaseUpdateSetupRequest();
    message.setupId = object.setupId ?? "";
    message.name = object.name ?? "";
    message.ownerId = object.ownerId ?? "";
    message.currentSetupVersion = (object.currentSetupVersion !== undefined && object.currentSetupVersion !== null)
      ? SetupVersion.fromPartial(object.currentSetupVersion)
      : undefined;
    message.status = object.status ?? 0;
    return message;
  },
};

function createBaseUpdateSetupResponse(): UpdateSetupResponse {
  return { success: false, setup: undefined };
}

export const UpdateSetupResponse: MessageFns<UpdateSetupResponse> = {
  encode(message: UpdateSetupResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.success !== false) {
      writer.uint32(8).bool(message.success);
    }
    if (message.setup !== undefined) {
      Setup.encode(message.setup, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UpdateSetupResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpdateSetupResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.setup = Setup.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UpdateSetupResponse {
    return {
      success: isSet(object.success) ? globalThis.Boolean(object.success) : false,
      setup: isSet(object.setup) ? Setup.fromJSON(object.setup) : undefined,
    };
  },

  toJSON(message: UpdateSetupResponse): unknown {
    const obj: any = {};
    if (message.success !== false) {
      obj.success = message.success;
    }
    if (message.setup !== undefined) {
      obj.setup = Setup.toJSON(message.setup);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UpdateSetupResponse>, I>>(base?: I): UpdateSetupResponse {
    return UpdateSetupResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UpdateSetupResponse>, I>>(object: I): UpdateSetupResponse {
    const message = createBaseUpdateSetupResponse();
    message.success = object.success ?? false;
    message.setup = (object.setup !== undefined && object.setup !== null) ? Setup.fromPartial(object.setup) : undefined;
    return message;
  },
};

function createBaseDeleteSetupRequest(): DeleteSetupRequest {
  return { setupId: "" };
}

export const DeleteSetupRequest: MessageFns<DeleteSetupRequest> = {
  encode(message: DeleteSetupRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.setupId !== "") {
      writer.uint32(10).string(message.setupId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeleteSetupRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeleteSetupRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.setupId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeleteSetupRequest {
    return { setupId: isSet(object.setup_id) ? globalThis.String(object.setup_id) : "" };
  },

  toJSON(message: DeleteSetupRequest): unknown {
    const obj: any = {};
    if (message.setupId !== "") {
      obj.setup_id = message.setupId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DeleteSetupRequest>, I>>(base?: I): DeleteSetupRequest {
    return DeleteSetupRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DeleteSetupRequest>, I>>(object: I): DeleteSetupRequest {
    const message = createBaseDeleteSetupRequest();
    message.setupId = object.setupId ?? "";
    return message;
  },
};

function createBaseDeleteSetupResponse(): DeleteSetupResponse {
  return { success: false };
}

export const DeleteSetupResponse: MessageFns<DeleteSetupResponse> = {
  encode(message: DeleteSetupResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.success !== false) {
      writer.uint32(8).bool(message.success);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeleteSetupResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeleteSetupResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeleteSetupResponse {
    return { success: isSet(object.success) ? globalThis.Boolean(object.success) : false };
  },

  toJSON(message: DeleteSetupResponse): unknown {
    const obj: any = {};
    if (message.success !== false) {
      obj.success = message.success;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DeleteSetupResponse>, I>>(base?: I): DeleteSetupResponse {
    return DeleteSetupResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DeleteSetupResponse>, I>>(object: I): DeleteSetupResponse {
    const message = createBaseDeleteSetupResponse();
    message.success = object.success ?? false;
    return message;
  },
};

function createBaseCreateSetupVersionRequest(): CreateSetupVersionRequest {
  return { setupId: "", version: "", content: undefined };
}

export const CreateSetupVersionRequest: MessageFns<CreateSetupVersionRequest> = {
  encode(message: CreateSetupVersionRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.setupId !== "") {
      writer.uint32(10).string(message.setupId);
    }
    if (message.version !== "") {
      writer.uint32(18).string(message.version);
    }
    if (message.content !== undefined) {
      Struct.encode(Struct.wrap(message.content), writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateSetupVersionRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateSetupVersionRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.setupId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.version = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.content = Struct.unwrap(Struct.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateSetupVersionRequest {
    return {
      setupId: isSet(object.setup_id) ? globalThis.String(object.setup_id) : "",
      version: isSet(object.version) ? globalThis.String(object.version) : "",
      content: isObject(object.content) ? object.content : undefined,
    };
  },

  toJSON(message: CreateSetupVersionRequest): unknown {
    const obj: any = {};
    if (message.setupId !== "") {
      obj.setup_id = message.setupId;
    }
    if (message.version !== "") {
      obj.version = message.version;
    }
    if (message.content !== undefined) {
      obj.content = message.content;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CreateSetupVersionRequest>, I>>(base?: I): CreateSetupVersionRequest {
    return CreateSetupVersionRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CreateSetupVersionRequest>, I>>(object: I): CreateSetupVersionRequest {
    const message = createBaseCreateSetupVersionRequest();
    message.setupId = object.setupId ?? "";
    message.version = object.version ?? "";
    message.content = object.content ?? undefined;
    return message;
  },
};

function createBaseCreateSetupVersionResponse(): CreateSetupVersionResponse {
  return { success: false, setupVersion: undefined };
}

export const CreateSetupVersionResponse: MessageFns<CreateSetupVersionResponse> = {
  encode(message: CreateSetupVersionResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.success !== false) {
      writer.uint32(8).bool(message.success);
    }
    if (message.setupVersion !== undefined) {
      SetupVersion.encode(message.setupVersion, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateSetupVersionResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateSetupVersionResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.setupVersion = SetupVersion.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateSetupVersionResponse {
    return {
      success: isSet(object.success) ? globalThis.Boolean(object.success) : false,
      setupVersion: isSet(object.setup_version) ? SetupVersion.fromJSON(object.setup_version) : undefined,
    };
  },

  toJSON(message: CreateSetupVersionResponse): unknown {
    const obj: any = {};
    if (message.success !== false) {
      obj.success = message.success;
    }
    if (message.setupVersion !== undefined) {
      obj.setup_version = SetupVersion.toJSON(message.setupVersion);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CreateSetupVersionResponse>, I>>(base?: I): CreateSetupVersionResponse {
    return CreateSetupVersionResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CreateSetupVersionResponse>, I>>(object: I): CreateSetupVersionResponse {
    const message = createBaseCreateSetupVersionResponse();
    message.success = object.success ?? false;
    message.setupVersion = (object.setupVersion !== undefined && object.setupVersion !== null)
      ? SetupVersion.fromPartial(object.setupVersion)
      : undefined;
    return message;
  },
};

function createBaseGetSetupVersionRequest(): GetSetupVersionRequest {
  return { setupVersionId: "" };
}

export const GetSetupVersionRequest: MessageFns<GetSetupVersionRequest> = {
  encode(message: GetSetupVersionRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.setupVersionId !== "") {
      writer.uint32(10).string(message.setupVersionId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetSetupVersionRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetSetupVersionRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.setupVersionId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetSetupVersionRequest {
    return { setupVersionId: isSet(object.setup_version_id) ? globalThis.String(object.setup_version_id) : "" };
  },

  toJSON(message: GetSetupVersionRequest): unknown {
    const obj: any = {};
    if (message.setupVersionId !== "") {
      obj.setup_version_id = message.setupVersionId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetSetupVersionRequest>, I>>(base?: I): GetSetupVersionRequest {
    return GetSetupVersionRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetSetupVersionRequest>, I>>(object: I): GetSetupVersionRequest {
    const message = createBaseGetSetupVersionRequest();
    message.setupVersionId = object.setupVersionId ?? "";
    return message;
  },
};

function createBaseGetSetupVersionResponse(): GetSetupVersionResponse {
  return { setupVersion: undefined };
}

export const GetSetupVersionResponse: MessageFns<GetSetupVersionResponse> = {
  encode(message: GetSetupVersionResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.setupVersion !== undefined) {
      SetupVersion.encode(message.setupVersion, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetSetupVersionResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetSetupVersionResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.setupVersion = SetupVersion.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetSetupVersionResponse {
    return { setupVersion: isSet(object.setup_version) ? SetupVersion.fromJSON(object.setup_version) : undefined };
  },

  toJSON(message: GetSetupVersionResponse): unknown {
    const obj: any = {};
    if (message.setupVersion !== undefined) {
      obj.setup_version = SetupVersion.toJSON(message.setupVersion);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetSetupVersionResponse>, I>>(base?: I): GetSetupVersionResponse {
    return GetSetupVersionResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetSetupVersionResponse>, I>>(object: I): GetSetupVersionResponse {
    const message = createBaseGetSetupVersionResponse();
    message.setupVersion = (object.setupVersion !== undefined && object.setupVersion !== null)
      ? SetupVersion.fromPartial(object.setupVersion)
      : undefined;
    return message;
  },
};

function createBaseSearchSetupVersionsRequest(): SearchSetupVersionsRequest {
  return { setupId: "", version: undefined };
}

export const SearchSetupVersionsRequest: MessageFns<SearchSetupVersionsRequest> = {
  encode(message: SearchSetupVersionsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.setupId !== "") {
      writer.uint32(10).string(message.setupId);
    }
    if (message.version !== undefined) {
      writer.uint32(18).string(message.version);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SearchSetupVersionsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSearchSetupVersionsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.setupId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.version = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SearchSetupVersionsRequest {
    return {
      setupId: isSet(object.setup_id) ? globalThis.String(object.setup_id) : "",
      version: isSet(object.version) ? globalThis.String(object.version) : undefined,
    };
  },

  toJSON(message: SearchSetupVersionsRequest): unknown {
    const obj: any = {};
    if (message.setupId !== "") {
      obj.setup_id = message.setupId;
    }
    if (message.version !== undefined) {
      obj.version = message.version;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SearchSetupVersionsRequest>, I>>(base?: I): SearchSetupVersionsRequest {
    return SearchSetupVersionsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SearchSetupVersionsRequest>, I>>(object: I): SearchSetupVersionsRequest {
    const message = createBaseSearchSetupVersionsRequest();
    message.setupId = object.setupId ?? "";
    message.version = object.version ?? undefined;
    return message;
  },
};

function createBaseSearchSetupVersionsResponse(): SearchSetupVersionsResponse {
  return { setupVersions: [] };
}

export const SearchSetupVersionsResponse: MessageFns<SearchSetupVersionsResponse> = {
  encode(message: SearchSetupVersionsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.setupVersions) {
      SetupVersion.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SearchSetupVersionsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSearchSetupVersionsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.setupVersions.push(SetupVersion.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SearchSetupVersionsResponse {
    return {
      setupVersions: globalThis.Array.isArray(object?.setup_versions)
        ? object.setup_versions.map((e: any) => SetupVersion.fromJSON(e))
        : [],
    };
  },

  toJSON(message: SearchSetupVersionsResponse): unknown {
    const obj: any = {};
    if (message.setupVersions?.length) {
      obj.setup_versions = message.setupVersions.map((e) => SetupVersion.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SearchSetupVersionsResponse>, I>>(base?: I): SearchSetupVersionsResponse {
    return SearchSetupVersionsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SearchSetupVersionsResponse>, I>>(object: I): SearchSetupVersionsResponse {
    const message = createBaseSearchSetupVersionsResponse();
    message.setupVersions = object.setupVersions?.map((e) => SetupVersion.fromPartial(e)) || [];
    return message;
  },
};

function createBaseUpdateSetupVersionRequest(): UpdateSetupVersionRequest {
  return { setupVersionId: "", version: "", content: undefined };
}

export const UpdateSetupVersionRequest: MessageFns<UpdateSetupVersionRequest> = {
  encode(message: UpdateSetupVersionRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.setupVersionId !== "") {
      writer.uint32(10).string(message.setupVersionId);
    }
    if (message.version !== "") {
      writer.uint32(18).string(message.version);
    }
    if (message.content !== undefined) {
      Struct.encode(Struct.wrap(message.content), writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UpdateSetupVersionRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpdateSetupVersionRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.setupVersionId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.version = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.content = Struct.unwrap(Struct.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UpdateSetupVersionRequest {
    return {
      setupVersionId: isSet(object.setup_version_id) ? globalThis.String(object.setup_version_id) : "",
      version: isSet(object.version) ? globalThis.String(object.version) : "",
      content: isObject(object.content) ? object.content : undefined,
    };
  },

  toJSON(message: UpdateSetupVersionRequest): unknown {
    const obj: any = {};
    if (message.setupVersionId !== "") {
      obj.setup_version_id = message.setupVersionId;
    }
    if (message.version !== "") {
      obj.version = message.version;
    }
    if (message.content !== undefined) {
      obj.content = message.content;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UpdateSetupVersionRequest>, I>>(base?: I): UpdateSetupVersionRequest {
    return UpdateSetupVersionRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UpdateSetupVersionRequest>, I>>(object: I): UpdateSetupVersionRequest {
    const message = createBaseUpdateSetupVersionRequest();
    message.setupVersionId = object.setupVersionId ?? "";
    message.version = object.version ?? "";
    message.content = object.content ?? undefined;
    return message;
  },
};

function createBaseUpdateSetupVersionResponse(): UpdateSetupVersionResponse {
  return { success: false, setupVersion: undefined };
}

export const UpdateSetupVersionResponse: MessageFns<UpdateSetupVersionResponse> = {
  encode(message: UpdateSetupVersionResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.success !== false) {
      writer.uint32(8).bool(message.success);
    }
    if (message.setupVersion !== undefined) {
      SetupVersion.encode(message.setupVersion, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UpdateSetupVersionResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpdateSetupVersionResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.setupVersion = SetupVersion.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UpdateSetupVersionResponse {
    return {
      success: isSet(object.success) ? globalThis.Boolean(object.success) : false,
      setupVersion: isSet(object.setup_version) ? SetupVersion.fromJSON(object.setup_version) : undefined,
    };
  },

  toJSON(message: UpdateSetupVersionResponse): unknown {
    const obj: any = {};
    if (message.success !== false) {
      obj.success = message.success;
    }
    if (message.setupVersion !== undefined) {
      obj.setup_version = SetupVersion.toJSON(message.setupVersion);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UpdateSetupVersionResponse>, I>>(base?: I): UpdateSetupVersionResponse {
    return UpdateSetupVersionResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UpdateSetupVersionResponse>, I>>(object: I): UpdateSetupVersionResponse {
    const message = createBaseUpdateSetupVersionResponse();
    message.success = object.success ?? false;
    message.setupVersion = (object.setupVersion !== undefined && object.setupVersion !== null)
      ? SetupVersion.fromPartial(object.setupVersion)
      : undefined;
    return message;
  },
};

function createBaseDeleteSetupVersionRequest(): DeleteSetupVersionRequest {
  return { setupVersionId: "" };
}

export const DeleteSetupVersionRequest: MessageFns<DeleteSetupVersionRequest> = {
  encode(message: DeleteSetupVersionRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.setupVersionId !== "") {
      writer.uint32(10).string(message.setupVersionId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeleteSetupVersionRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeleteSetupVersionRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.setupVersionId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeleteSetupVersionRequest {
    return { setupVersionId: isSet(object.setup_version_id) ? globalThis.String(object.setup_version_id) : "" };
  },

  toJSON(message: DeleteSetupVersionRequest): unknown {
    const obj: any = {};
    if (message.setupVersionId !== "") {
      obj.setup_version_id = message.setupVersionId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DeleteSetupVersionRequest>, I>>(base?: I): DeleteSetupVersionRequest {
    return DeleteSetupVersionRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DeleteSetupVersionRequest>, I>>(object: I): DeleteSetupVersionRequest {
    const message = createBaseDeleteSetupVersionRequest();
    message.setupVersionId = object.setupVersionId ?? "";
    return message;
  },
};

function createBaseDeleteSetupVersionResponse(): DeleteSetupVersionResponse {
  return { success: false };
}

export const DeleteSetupVersionResponse: MessageFns<DeleteSetupVersionResponse> = {
  encode(message: DeleteSetupVersionResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.success !== false) {
      writer.uint32(8).bool(message.success);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeleteSetupVersionResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeleteSetupVersionResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeleteSetupVersionResponse {
    return { success: isSet(object.success) ? globalThis.Boolean(object.success) : false };
  },

  toJSON(message: DeleteSetupVersionResponse): unknown {
    const obj: any = {};
    if (message.success !== false) {
      obj.success = message.success;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DeleteSetupVersionResponse>, I>>(base?: I): DeleteSetupVersionResponse {
    return DeleteSetupVersionResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DeleteSetupVersionResponse>, I>>(object: I): DeleteSetupVersionResponse {
    const message = createBaseDeleteSetupVersionResponse();
    message.success = object.success ?? false;
    return message;
  },
};

function createBaseListSetupsRequest(): ListSetupsRequest {
  return { organisationId: undefined, ownerId: undefined, limit: 0, offset: 0 };
}

export const ListSetupsRequest: MessageFns<ListSetupsRequest> = {
  encode(message: ListSetupsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.organisationId !== undefined) {
      writer.uint32(10).string(message.organisationId);
    }
    if (message.ownerId !== undefined) {
      writer.uint32(18).string(message.ownerId);
    }
    if (message.limit !== 0) {
      writer.uint32(24).int32(message.limit);
    }
    if (message.offset !== 0) {
      writer.uint32(32).int32(message.offset);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListSetupsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListSetupsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.organisationId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.ownerId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.limit = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.offset = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListSetupsRequest {
    return {
      organisationId: isSet(object.organisation_id) ? globalThis.String(object.organisation_id) : undefined,
      ownerId: isSet(object.owner_id) ? globalThis.String(object.owner_id) : undefined,
      limit: isSet(object.limit) ? globalThis.Number(object.limit) : 0,
      offset: isSet(object.offset) ? globalThis.Number(object.offset) : 0,
    };
  },

  toJSON(message: ListSetupsRequest): unknown {
    const obj: any = {};
    if (message.organisationId !== undefined) {
      obj.organisation_id = message.organisationId;
    }
    if (message.ownerId !== undefined) {
      obj.owner_id = message.ownerId;
    }
    if (message.limit !== 0) {
      obj.limit = Math.round(message.limit);
    }
    if (message.offset !== 0) {
      obj.offset = Math.round(message.offset);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListSetupsRequest>, I>>(base?: I): ListSetupsRequest {
    return ListSetupsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListSetupsRequest>, I>>(object: I): ListSetupsRequest {
    const message = createBaseListSetupsRequest();
    message.organisationId = object.organisationId ?? undefined;
    message.ownerId = object.ownerId ?? undefined;
    message.limit = object.limit ?? 0;
    message.offset = object.offset ?? 0;
    return message;
  },
};

function createBaseListSetupsResponse(): ListSetupsResponse {
  return { setups: [], totalCount: 0 };
}

export const ListSetupsResponse: MessageFns<ListSetupsResponse> = {
  encode(message: ListSetupsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.setups) {
      Setup.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.totalCount !== 0) {
      writer.uint32(16).int32(message.totalCount);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListSetupsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListSetupsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.setups.push(Setup.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.totalCount = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListSetupsResponse {
    return {
      setups: globalThis.Array.isArray(object?.setups) ? object.setups.map((e: any) => Setup.fromJSON(e)) : [],
      totalCount: isSet(object.total_count) ? globalThis.Number(object.total_count) : 0,
    };
  },

  toJSON(message: ListSetupsResponse): unknown {
    const obj: any = {};
    if (message.setups?.length) {
      obj.setups = message.setups.map((e) => Setup.toJSON(e));
    }
    if (message.totalCount !== 0) {
      obj.total_count = Math.round(message.totalCount);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListSetupsResponse>, I>>(base?: I): ListSetupsResponse {
    return ListSetupsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListSetupsResponse>, I>>(object: I): ListSetupsResponse {
    const message = createBaseListSetupsResponse();
    message.setups = object.setups?.map((e) => Setup.fromPartial(e)) || [];
    message.totalCount = object.totalCount ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000).toString();
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (globalThis.Number(t.seconds) || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
