// Copyright 2025 DigitalKin Inc.
//
// Licensed under the GNU General Public License, Version 3.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.gnu.org/licenses/gpl-3.0.html
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

syntax = "proto3";

package agentic_mesh_protocol.setup.v1;

import "buf/validate/validate.proto";
import "google/protobuf/struct.proto";
import "google/protobuf/timestamp.proto";

// ═════════════════════════════════════ Objects ════════════════════════════════════ //

// SetupStatus represents the status of a setup.
enum SetupStatus {
  // DRAFT indicates the setup is a draft.
  DRAFT = 0;
  // VALIDATING indicates the setup is being validated.
  VALIDATING = 1;
  // READY indicates the setup is ready.
  READY = 2;
  // PAUSED indicates the setup is paused.
  PAUSED = 3;
  // FAILED indicates the setup failed.
  FAILED = 4;
  // ARCHIVED indicates the setup has been archived.
  ARCHIVED = 5;
  // NEEDS_CONFIGURATION indicates the setup needs configuration.
  NEEDS_CONFIGURATION = 6;
  // CONFIGURATION_FAILED indicates the setup configuration failed.
  CONFIGURATION_FAILED = 7;
  // CONFIGURATION_SUCCEEDED indicates the setup configuration succeeded.
  CONFIGURATION_SUCCEEDED = 8;
}

// SetupVersion represents a version of a setup's configuration.
message SetupVersion {
  // id is the unique identifier of the setup version.
  string id = 1 [
    (buf.validate.field).required = true,
    (buf.validate.field).string.prefix = 'setup_versions:'
  ];

  // setup_id is the unique identifier of the parent setup.
  string setup_id = 2 [
    (buf.validate.field).required = true,
    (buf.validate.field).string.prefix = 'setups:'
  ];

  // version is the label for this version (e.g., "v1", "v2").
  string version = 3 [(buf.validate.field).required = true];

  // content contains key-value pairs for configuration content.
  google.protobuf.Struct content = 4 [(buf.validate.field).required = true];

  // creation_date is the setup version creation date.
  google.protobuf.Timestamp creation_at = 5;
}

// Setup represents a configuration associated with a mission.
message Setup {
  // id is the unique identifier of the setup.
  string id = 1 [
    (buf.validate.field).required = true,
    (buf.validate.field).string.prefix = "setups:"
  ];

  // name is the name of the setup.
  string name = 2 [(buf.validate.field).required = true];

  // organisation_id is the unique identifier of the organisation.
  string organization_id = 3 [(buf.validate.field).required = true, (buf.validate.field).string.prefix = "organizations_id:"];

  // owner_id is the owner ID of the setup.
  string owner_id = 4 [(buf.validate.field).required = true, (buf.validate.field).string.prefix = 'users:'];

  // module_id is the module identifier associated with the setup.
  string module_id = 5 [(buf.validate.field).required = true, (buf.validate.field).string.prefix = 'modules:'];

  // current_setup_version is the active setup version.
  optional SetupVersion current_setup_version = 6;

  // status is the status of the setup.
  SetupStatus status = 7 [(buf.validate.field).required = true];
}
